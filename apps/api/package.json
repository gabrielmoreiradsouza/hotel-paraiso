{
  "name": "@hotel-paraiso/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^5.4.0",
    "@fastify/cors": "^11.0.0"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
